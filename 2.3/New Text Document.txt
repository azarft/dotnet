You can’t manually delete an object in C# because memory is managed by the Garbage Collector. It automatically removes unused objects.

A memory leak in managed code happens when an object is no longer needed but is still referenced, so the Garbage Collector cannot free it.