1. ++i увеличивает переменную сначала и возвращает новое значение, а i++ возвращает старое значение, а увеличивает уже после.

2. % возвращает остаток от деления; полезен для проверки чётности (n % 2 == 0), циклических индексов и работы с периодичностью.