1. Нет, в классическом switch значения case должны быть константами, а не переменными.

2. switch эффективнее, когда проверяется одно выражение на множество фиксированных значений (меню, команды, состояния), делая код чище и быстрее, чем длинная цепочка if-else.